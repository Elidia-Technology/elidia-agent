name: web-xai
version: 1.0.0
description: "xAI Web Search — search the web via Grok's agentic web_search tool (Responses API). Requires xAI Grok OAuth (via `elidia auth`) or XAI_API_KEY (https://x.ai)."
author: AiUtils
kind: backend
provides_web_providers:
  - xai
